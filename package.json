{
  "name": "layout-grid-visualizer",
  "version": "1.2.0",
  "private": true,
  "description": "Layout Grid Visualizer Chrome Extension - Build tools",
  "scripts": {
    "build": "node build.js",
    "zip": "node build.js --zip",
    "clean": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.rmSync('layout-grid-visualizer.zip',{force:true});console.log('Cleaned.')\""
  },
  "devDependencies": {
    "lightningcss": "^1.29.1",
    "terser": "^5.37.0"
  }
}
